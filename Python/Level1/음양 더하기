def solution(absolutes, signs):
    num = 0
    answer = 0
    for i in range(len(absolutes)):
        if signs[i] == True:
            num = absolutes[i]
        else:
            num = absolutes[i] * (-1)
        answer += num
    return answer
